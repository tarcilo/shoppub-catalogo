# Token do endpoint de sync diário (/api/sync). Gere um valor aleatório.
# Na Vercel, use o mesmo valor de CRON_SECRET (o Cron envia automaticamente
# o header Authorization: Bearer <CRON_SECRET>).
SYNC_TOKEN=
